# Dependencies
node_modules/

# Build output
dist/
.angular/

# Runtime data (videos, clips, tokens)
.data/
.data-e2e/

# Environment. The bare `.env` pattern does not match `.env.prod`, which holds
# the session secret and OAuth credentials — match every variant but keep the
# committed *.example templates.
.env
.env.*
!.env.*.example

# Test artifacts
coverage/
playwright-report/
test-results/

# OS / editor
.DS_Store
.gitnexus
